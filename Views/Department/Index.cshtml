@model IEnumerable<Assi7.Models.Department>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.DeptName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.DeptName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { deptId=item.DeptId }) |
            @Html.ActionLink("Details", "Details", new { deptId = item.DeptId }) |
            @Html.ActionLink("Delete", "Delete", new { deptId = item.DeptId })
        </td>
    </tr>
}

</table>
